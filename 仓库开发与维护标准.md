# WBMM 仓库开发与维护标准

> 适用仓库：`/home/a/WBMM`  
> 文档级别：工程治理标准  
> 当前基线：`test` 分支，提交 `0e9b7e5`（2026-08-31 核对）  
> 适用对象：项目负责人、协作者、Codex/Claude 等代码代理

## 1. 文档目的

本标准解决两个问题：

1. 明确当前仓库存在的结构、接口、验证和维护问题，以及每类问题的修改方法；
2. 规定以后增加任务、规划器、控制器、传感器、仿真场景和实机功能时必须遵守的流程。

本标准不替代具体技术合同。以下文档的优先级为：

1. [`architecture.md`](architecture.md)：系统数据流、状态、控制与参考所有权合同；
2. [`frames.md`](frames.md)：坐标系、变换与物理量表达合同；
3. [`ta_wbmp/TASK_SCHEMA.md`](../src/algorithms/planning/ta_wbmp/TASK_SCHEMA.md)：统一任务 YAML 合同；
4. 本文：仓库分层、开发流程、验证、供应商代码和发布标准；
5. 各功能包 README：包内实现、参数与使用方法。

若实现与合同冲突，不允许默默兼容。必须选择以下一种做法：

- 修正实现，使其重新满足合同；
- 提交 ADR（架构决策记录），说明为什么要修改合同、影响范围、迁移方法和回退方案，然后同步修改文档与代码。

---

## 2. 项目负责人必须掌握的五项权力

代码可以由代理大量实现，但下列内容必须由项目负责人确认：

1. **架构权**：模块为什么存在、属于哪一层、可以依赖谁；
2. **接口权**：状态维度、消息字段、单位、frame、topic/service/action 和所有权；
3. **配置权**：哪些是任务参数、算法参数、设备参数和安全参数；
4. **验收权**：什么证据可以称为“已完成”，什么只能称为“部分完成”；
5. **发布权**：哪个提交是仿真基线、哪个提交允许进入实机验证。

任何代码代理开始修改前，负责人至少应能回答：

- 输入是什么，输出是什么？
- 由哪个模块负责，为什么不是另一个模块？
- 失败时系统处于什么安全状态？
- 如何在 MuJoCo 中复现？
- 哪些测试通过后才允许合并？

---

## 3. 统一目标架构

### 3.1 主链

```text
统一任务 YAML
    ↓
TaskTrajectoryGenerator
    ↓  TaskTrajectory（任务空间约束轨迹）
TA-WBMP
    ↓  Plan（q_pre、q_entry、tau_task、指标与失败原因）
ExecutionCoordinator
    ├─ NAVIGATING：发送 q_pre → REMANI → OCS2
    └─ TASK_EXEC：发送 tau_task → 可选力控修正 → OCS2 MPC/MRT
                                                        ↓
                                                   MuJoCo / 实机
```

### 3.2 OCS2 与 REMANI 的组织原则

推荐“**一个 WBMM 框架、两个独立求解后端**”，不推荐把 OCS2 和 REMANI 的源码合并为一个大包。

必须统一的是：

- 9D 全身状态和 8D 控制输入；
- 机器人模型、关节顺序、frame 与单位；
- 环境/ESDF 接口和碰撞安全语义；
- 规划结果、执行状态、失败原因和时间戳；
- 顶层启动入口、日志、实验产物与验收方法。

必须保持独立的是：

- REMANI 的搜索、轨迹优化、ESDF 查询与重规划内部实现；
- OCS2 的动力学、代价、约束、SLQ/DDP、MPC/MRT 内部实现；
- 两者各自的上游更新和补丁历史。

目录目标：

```text
src/
├── vendor/
│   ├── ocs2_ros2/                 # 上游求解器，少改
│   └── remani_planner/            # 上游/项目 fork，补丁可追踪
├── interfaces/
│   └── tracer_jaka_interfaces/    # 稳定跨包协议
├── algorithms/
│   ├── planning/
│   │   ├── ta_wbmp/
│   │   └── remani_adapter/        # 目标形态；不放 REMANI 内核
│   ├── control/
│   │   ├── ocs2_adapter/          # 目标形态；不放 OCS2 内核
│   │   └── whole_body_force_control/
│   ├── environment/
│   │   └── wbmm_environment/
│   └── execution/
│       └── wbmm_coordinator/
├── applications/
│   └── wiping/
├── simulation/
│   └── tracer_jaka_mujoco/
└── bringup/
    └── tracer_jaka_bringup/       # 唯一系统组合入口
```

这是目标形态，不代表当前已经全部完成。迁移应小步进行，不能为了目录整齐一次性改坏可运行基线。

### 3.3 不可改变的基础契约

全身状态：

```text
x = [base_x, base_y, base_yaw, joint_1, ..., joint_6]  # 9D
```

控制输入：

```text
u = [base_v, base_yaw_rate, joint_velocity_1, ..., joint_velocity_6]  # 8D
```

差速底盘不允许横向速度。所有物理量必须明确单位和 frame。任务几何默认在规划坐标系表达，力传感器数据默认在传感器坐标系表达，未经显式变换不得混用。

同一时刻只能有一个 `/mobile_manipulator_mpc_target` 参考所有者：

- `NAVIGATING`：REMANI bridge；
- `TASK_EXEC`：ExecutionCoordinator；
- 所有权切换必须有明确请求、确认与超时，不能靠两个发布器“恰好错开”。

---

## 4. 当前存在的问题与修改方法

下表区分“当前证据”和“目标修改”。仅凭文档或源码存在，不代表完整闭环已经验证。

| 优先级 | 当前问题 | 风险 | 修改方法 | 完成判据 |
|---|---|---|---|---|
| P0 | 参考所有权的文字与实现边界仍有歧义 | 两个节点同时发 MPC 目标，导致跳变或失控 | 将所有权定义为“按阶段唯一”；由 Coordinator 完成显式交权；修正 TA-WBMP 文档中“始终只有 Coordinator 发布”的表述 | 运行时检查目标 topic 发布者数量；NAVIGATING/TASK_EXEC 切换测试通过 |
| P0 | TA-WBMP 默认碰撞检查仍可使用 `AcceptAllStateValidityChecker` | 任务轨迹在 RViz 可见但可能与桌面、机械臂或环境碰撞 | 实现 `wbmm_environment`/REMANI ESDF adapter；TA-WBMP 与 REMANI 共用机器人碰撞几何、安全距离和 unknown-space 策略 | 碰撞目标、插值接近段和完整任务段均被拒绝；MuJoCo 无穿透 |
| P0 | “能启动”容易被误认为“任务已完成” | 产生错误科研结论或直接进入实机 | 建立第 9 节验证矩阵；报告中强制区分源码检查、构建、离线规划、MuJoCo 闭环、实机验证 | 每个结论附 Git SHA、配置、命令、日志和指标 |
| P0 | 实机与仿真启动入口仍可能重复启动 TF、bridge、MPC 或控制输出 | TF 冲突、参考冲突、意外运动 | `tracer_jaka_bringup` 成为唯一系统组合层；算法包 launch 只启动本包节点 | 顶层 launch 图中每个 TF/topic/控制输出只有一个 owner |
| P0 | 安全放行依赖多个松散参数 | 误把只读模式当作底盘也不动 | 实机默认 `execution_enabled=false`、`command_output_enabled=false`、`jaka_read_only=true`、`force_control_enabled=false`、`auto_start=false`；增加统一 readiness gate | 未显式放行时底盘和机械臂均无运动命令 |
| P1 | `tracer_jaka_interfaces` 目前仅为骨架，状态大量用 `std_msgs/String` | 状态拼写漂移、无法机器校验、错误信息不结构化 | 增加 `PipelineStatus`、`ForceControlStatus`、`PlanningResult`/错误码，按版本迁移 | 状态、阶段、错误码、owner、时间戳可由测试直接断言 |
| P1 | `tracer_jaka_ocs2` 同时承担算法适配、ROS 节点和整套系统启动 | 高耦合，修改 MPC 容易影响仿真/REMANI/建图 | 先在包内分 `core/adapter/nodes`，再把完整 sim/real launch 移至 bringup | 算法包不再依赖 MuJoCo、REMANI、grid_map 的系统组合 |
| P1 | `remani_integration`、`contact_control_core` 是占位目录，而实际实现位于其他包 | 新协作者找错入口，形成重复实现 | 要么落实为真实 adapter/core 包，要么删除占位并在文档指向现有实现；不保留长期空壳 | 每个目录有明确 owner、接口、测试或被删除 |
| P1 | URDF/xacro 在 description、MuJoCo、OCS2、MoveIt 中重复 | 关节、碰撞体、工具长度和 frame 漂移 | `tracer_jaka_description` 作为唯一源；仿真/实机仅通过参数和 ros2_control 插件覆盖 | CI 比较 joint/frame/limit；所有后端从同一描述生成 |
| P1 | vendor REMANI 已含项目定制，但缺少补丁账本 | 无法升级上游，也无法解释为何修改 | 新建 `docs/vendor_patches.md`；记录上游版本、补丁提交、原因、测试和回退；深度修改时维护正式 fork | 可从干净上游重放补丁并通过 REMANI 回归测试 |
| P1 | 统一任务 YAML 与 OCS2/REMANI 配置边界容易混乱 | 一个大 YAML 同时承载算法内部细节，难升级 | 任务 YAML 只描述任务、约束和执行意图；OCS2 `task.info` 与 REMANI solver 配置保持独立，由 profile 引用 | 同一任务可更换 solver profile，不改任务几何 |
| P1 | 重规划阈值、MPC 小误差补偿和故障恢复边界未形成统一协议 | 小误差频繁重规划，或大误差不恢复 | 设 tracking/force/replan 三级滞回；小误差由 MPC/力控吸收，持续大误差或碰撞风险才触发 REMANI | 注入小扰动不重规划；持续超阈值按规定重规划/停机 |
| P1 | 力控模型已独立，但接触监督仍有历史逻辑散落在 WipePlanner/Coordinator | 迁移后可能丢失 guarded approach、首触捕获、暂停和硬限位 | 定义独立 `ContactSupervisor`，迁移时保留 WipePlanner 作为行为基线并做对比测试 | 无力、正常接触、尖峰、超力、传感器超时场景全部通过 |
| P2 | 多个核心源文件超过 1000 行 | 难审查、难单测、代理修改容易产生隐式副作用 | 按“纯核心/ROS adapter/状态机/安全/可视化”拆分，不按行数机械切文件 | 每个单元职责单一；核心逻辑可不启动 ROS 测试 |
| P2 | 尚未发现仓库级 CI、pre-commit、CONTRIBUTING、CODEOWNERS | 风格、构建和安全约束靠人工记忆 | 增加最小 CI、格式检查、`git diff --check`、选定包构建测试和贡献指南 | 新提交在合并前自动获得一致的验证结果 |
| P2 | 85 个 ROS 包会使全量构建慢且易受 vendor 影响 | 日常开发反馈慢，代理容易跳过测试 | 定义 package groups/profiles；日常只构建改动包及依赖，夜间/发布再全量 | 快速验证目标明确，发布验证覆盖全链 |
| P2 | 绝对路径、生成产物与现场地图可能混入配置 | 换机器无法复现 | 使用 package share、launch 参数和数据 manifest；大数据不进 Git，但记录校验和与生成方法 | 新机器按文档可恢复同一实验输入 |

### 4.1 对 WipePlanner 的处理标准

`wipe_planner` 不再承担新主链规划职责，但暂时不能直接删除。它应被视为：

- 接触安全监督的回归基线；
- 擦桌/擦板任务行为的对照实现；
- 尚未迁移能力的临时宿主。

只有同时满足以下条件才允许下线：

1. 统一 YAML 能表达现有 table/blackboard/RAS 任务；
2. TA-WBMP 能输出等价或更好的 `q_pre/q_entry/tau_task`；
3. REMANI 导航和交权闭环在 MuJoCo 复现；
4. guarded approach、接触捕获、进度节流、尖峰拒绝、硬限位和超时行为已迁出；
5. 新主链通过无力与有力回归；
6. 旧包的可复现实验数据和关键参数已归档。

删除前先标记 deprecated，至少保留一个稳定发布周期，不允许边迁移边删除唯一可用基线。

---

## 5. 模块边界标准

### 5.1 依赖方向

允许的依赖方向：

```text
applications / bringup
        ↓
execution / planning / control / perception
        ↓
interfaces / robot description / common core
        ↓
vendor libraries
```

simulation 和 drivers 是可替换后端，应实现相同 ROS 接口。算法层不得依赖具体设备 IP、串口、CAN 口或 MuJoCo 场景。

禁止：

- vendor 反向依赖项目 application；
- driver 读取擦拭 YAML；
- task generator 直接发布硬件命令；
- 规划器自行启动整套机器人；
- RViz Marker 被当成碰撞模型；
- 为了让规划成功而关闭碰撞或缩小真实碰撞体。

### 5.2 模块输入输出必须写成契约

每个新增模块的 README 至少写清：

```text
职责：只做什么，不做什么
输入：类型、维度、顺序、单位、frame、QoS、频率
输出：类型、维度、顺序、单位、frame、QoS、频率
所有权：谁能发布/调用，何时获得和释放
参数：默认值、范围、是否安全关键
失败：错误码、超时、降级和停机行为
验证：单测、MuJoCo、实机状态
```

### 5.3 ROS 接口规则

- topic 名只表达数据语义，不包含临时实验编号；
- 长流程优先使用 Action，开关/查询使用 Service，连续数据使用 Topic；
- 状态机不得长期依赖自由文本 String；
- 所有消息必须有时间戳，涉及空间量必须有 frame；
- 控制命令必须定义 freshness timeout；
- `transient_local` 只用于需要晚加入者获取最后状态的配置/可视化数据；
- 每个关键 topic 在文档中指定唯一 owner。

---

## 6. 配置与统一 YAML 标准

### 6.1 四类配置分离

| 配置类型 | 内容 | 示例位置 |
|---|---|---|
| 任务配置 | 表面、轨迹图案、速度、接触意图、任务约束 | `ta_wbmp/config/*.yaml` |
| 算法配置 | REMANI 搜索/优化、OCS2 动力学/代价、力控默认参数 | 各算法包 `config/defaults/` |
| 机器人配置 | URDF、关节限位、标定、碰撞几何 | `tracer_jaka_description` |
| 部署配置 | sim/real、设备、地图、是否输出命令 | `tracer_jaka_bringup/config/profiles/`（目标） |

统一 YAML 是“统一任务入口”，不是“把所有软件参数塞进一个文件”。推荐引用 profile：

```yaml
task:
  schema_version: 1
  name: table_wipe
  frame_id: odom
  # surface / pattern / approach / constraints ...

execution:
  planner_profile: remani_safe
  controller_profile: ocs2_tracking
  force_profile: disabled
```

### 6.2 Schema 规则

- 必须有 `schema_version`；
- 未知字段、缺失必填字段、错误维度必须在启动前报错；
- 数值必须说明 SI 单位；
- `frame_id` 不允许靠默认猜测；
- 安全参数不能由任务文件任意放宽，只允许部署 profile 收紧或经审查修改；
- schema 破坏性变化必须升级主版本并提供迁移工具或样例。

---

## 7. 新功能应该添加在哪里

| 新需求 | 应修改的位置 | 不应修改的位置 |
|---|---|---|
| 新擦拭形状/新任务轨迹 | 新 YAML；不足时新增 `TaskTrajectoryProvider` | REMANI、OCS2 内核 |
| 新候选评分指标 | `CandidateCostEvaluator` 实现与配置 | task generator |
| 新 IK/可达性方法 | TA-WBMP planner adapter/core | Coordinator、driver |
| 新碰撞地图/ESDF | `wbmm_environment` adapter | RViz Marker、任务 YAML 中的假障碍物 |
| 新导航规划器 | 新 `NavigationPlanner` adapter，输出统一规划结果 | OCS2 内核 |
| 新 MPC/控制器 | 新 controller adapter，消费统一 9D 参考 | TaskTrajectoryGenerator |
| 新恒力/导纳算法 | `whole_body_force_control` core + test | WipePlanner 私有代码 |
| 新接触安全策略 | `ContactSupervisor` | vendor solver |
| 新仿真场景 | `tracer_jaka_mujoco/models` + scenario profile | 算法包 launch |
| 新实机传感器 | `drivers/` + 标准消息 adapter | planning core |
| 新整机启动组合 | `tracer_jaka_bringup` | OCS2/TA-WBMP 包内的全系统 launch |
| 新可视化 | `wbmm_visualization`，消费统一数据契约 | 复制一套规划器私有 Marker 逻辑 |

### 7.1 新功能设计卡

实现前先提交一页设计卡，至少包含：

```text
问题：要解决什么可测量问题？
边界：修改哪些模块，不修改哪些模块？
接口：输入/输出/维度/单位/frame/owner？
约束：哪些是硬约束，哪些是软代价？
安全：失败时保持、减速、重规划还是停机？
复现：使用哪个 YAML、场景、随机种子和 Git SHA？
验收：数值指标与 Go/No-Go 门槛是什么？
回退：如何恢复旧行为？
```

没有设计卡，不进入大规模实现。

---

## 8. 标准开发流程

### 8.1 变更前

1. 阅读 `architecture.md`、`frames.md` 和相关包 README；
2. 检查当前分支、Git SHA 和未提交修改；
3. 运行或记录当前最小基线；
4. 判断变更属于 Bug 修复、参数调整、接口变化、重构还是新功能；
5. 写清验收标准；接口/架构变化先写 ADR。

### 8.2 实现中

- 一次提交只解决一个可解释问题；
- 先写纯核心和测试，再接 ROS adapter；
- 不修改无关文件，不覆盖他人的未提交变更；
- 不以关闭碰撞、放宽硬限位或缩小碰撞体换取“成功”；
- 参数变化必须记录旧值、新值、物理含义和依据；
- 所有 fallback 都必须显式记录，不能悄悄回退到伪数据或 AcceptAll；
- 新状态必须带失败原因，不能只输出 `failed`。

### 8.3 提交前

最小检查：

```bash
git diff --check
colcon build --packages-select <changed_packages> --symlink-install
colcon test --packages-select <changed_packages>
colcon test-result --verbose
```

规划/执行相关变更还必须运行对应离线场景；碰撞、参考所有权、力控或输出命令相关变更必须运行 MuJoCo 闭环。

### 8.4 提交信息

推荐格式：

```text
feat(ta_wbmp): add task-direction manipulability metric
fix(remani_adapter): suppress replanning inside MPC tolerance
refactor(ocs2_adapter): separate reference bridge from bringup
test(force_control): cover force spike and sensor timeout
docs(architecture): define reference ownership handoff
```

每个提交说明：为什么改、行为如何变化、验证了什么、尚未验证什么。

### 8.5 合并标准

以下任一情况不得合并到稳定分支：

- 测试失败但被描述为“环境问题”且没有证据；
- 修改接口却没有同步文档和消费者；
- 新增 topic 发布者但未检查所有权；
- 仿真存在碰撞穿透；
- 只在 RViz 看起来正确，没有状态/误差/接触指标；
- 实机相关功能仅完成构建，却声称实机验证通过；
- vendor 修改没有补丁记录。

---

## 9. 验证分级与完成定义

### 9.1 验证等级

| 等级 | 名称 | 必须提供的证据 |
|---|---|---|
| L0 | 合同检查 | schema、维度、frame、单位、owner、参数合法性 |
| L1 | 单元测试 | 核心函数、边界值、错误路径、确定性随机种子 |
| L2 | 离线规划 | YAML 输入、候选/失败原因、轨迹、碰撞检查、指标文件 |
| L3 | MuJoCo 无力闭环 | REMANI 到 q_pre、显式交权、OCS2 跟踪、COMPLETE、无碰撞 |
| L4 | MuJoCo 接触闭环 | 目标力、稳态误差、超调、接触保持、尖峰/超时/硬限测试 |
| L5 | 实机只读 | TF、状态、传感器、地图、topic owner、watchdog，无命令输出 |
| L6 | 实机运动 | 风险评审、急停、低速、分阶段放行、日志与复盘 |

### 9.2 “已完成”的定义

一个功能只有在其声明的等级全部通过时，才能称为“已完成”。报告必须使用以下词汇：

- **已实现**：代码存在，但可能未构建或运行；
- **已构建**：目标包构建成功；
- **已离线验证**：L0–L2 通过；
- **已在 MuJoCo 闭环验证**：相应 L3/L4 通过；
- **已实机只读验证**：L5 通过；
- **已实机运动验证**：L6 通过；
- **未验证**：明确列出缺失证据。

禁止用“launch 成功”“RViz 有轨迹”“节点没有崩溃”替代闭环完成。

### 9.3 每次实验必须保存

```text
experiment_id/
├── manifest.yaml       # Git SHA、分支、日期、主机、ROS/MuJoCo 版本
├── task.yaml           # 实际任务文件副本
├── profiles.yaml       # solver/controller/deployment 参数
├── summary.yaml        # 成功、失败阶段、错误码和核心指标
├── trajectory.csv      # 9D 参考与实测
├── wrench.csv          # 有接触时保存
└── logs/               # 节点日志或 rosbag 索引
```

至少记录：规划时间、路径长度、最小碰撞距离、最小关节裕度、最小奇异值、q_pre 到达误差、MPC RMS/最大误差、重规划次数、任务完成时间；力控开启时再记录稳态力误差、峰值力和接触丢失次数。

---

## 10. 重规划、MPC 与力控边界

原则：**小误差由 MPC/力控补偿，持续的大误差、路径失效或碰撞风险才重规划。**

推荐三级机制：

```text
正常区：error < tracking_slow_threshold
  → MPC 正常补偿，不重规划

缓冲区：tracking_slow_threshold ≤ error < tracking_stop_threshold
  → 降低任务虚拟进度，使用滞回，不重规划

故障区：error ≥ replan_threshold 且持续 T_confirm
  或路径碰撞/目标显著变化/控制失效
  → 暂停任务，安全保持，触发 REMANI 重规划或故障退出
```

必须满足：

- 阈值按位置、姿态、关节、力分别定义，不使用一个无单位总误差；
- 进入和退出阈值不同，避免抖动；
- 必须有连续样本或持续时间确认；
- 重规划有最小间隔和最大连续失败次数；
- 接触任务中的超力首先触发退让/停机，不直接重规划冲向表面；
- 每次重规划记录触发源、误差、持续时间和新旧目标。

---

## 11. Vendor 管理标准

### 11.1 基本原则

OCS2 和 REMANI 保留在 `src/vendor/`。项目定制优先写在 adapter，不直接侵入 solver core。

允许修改 vendor 的情况：

- 上游缺陷阻塞项目且无法通过 adapter 修复；
- 研究贡献本身就是对求解器内部算法的修改；
- 性能要求必须进入内部循环。

每次 vendor 修改必须记录：

```text
组件与上游版本/commit
项目补丁 commit
修改原因和对应问题
改动文件与关键接口
上游是否已有修复
回归测试
升级/回退方法
```

### 11.2 何时维护 REMANI fork

若只改少量 ROS 2 适配或 Bug，保留 vendor + patch ledger。若持续修改以下内容，则建立正式 fork：

- 状态/控制变量定义；
- Kino A* 搜索空间或运动原语；
- 动力学可行性约束；
- MINCO/轨迹优化目标；
- 重规划状态机；
- 对外消息协议的大范围变化。

即使建立 fork，WBMM 仍通过 adapter 使用它，不允许 application 直接依赖其内部类。

---

## 12. 分支、版本与发布

推荐分支：

- `main`：经过定义验证等级的稳定基线；
- `test`：当前集成验证分支；
- `codex/<topic>` 或 `feature/<topic>`：单一功能分支；
- `fix/<topic>`：缺陷修复；
- `experiment/<topic>`：不可作为稳定功能依赖的研究试验。

发布 tag 建议：

```text
sim-vX.Y.Z       # 至少 L3；接触功能至少 L4
real-ro-vX.Y.Z   # 至少 L5
real-vX.Y.Z      # 至少 L6
```

发布说明必须列出：功能、配置、验证等级、已知限制、地图/模型版本、回退提交。

---

## 13. 文档与 ADR 标准

### 13.1 唯一事实源

- 架构与数据流：`docs/architecture.md`；
- frame：`docs/frames.md`；
- 任务 schema：`ta_wbmp/TASK_SCHEMA.md`；
- 仿真/实机启动：bringup README 与 `docs/QUICKSTART.md`；
- 包内部参数与故障排查：包 README；
- 代理入口：`CLAUDE.md`，只做索引和强约束摘要，不复制大量易漂移细节。

### 13.2 必须写 ADR 的情况

- 改变 9D/8D 定义、关节顺序或主要 frame；
- 改变参考 owner 或状态机；
- 新增/替换规划器、控制器或碰撞后端；
- 改变任务 YAML schema；
- 将代码移入/移出 vendor；
- 删除 WipePlanner 或其他兼容基线；
- 放宽安全阈值或改变实机默认输出策略。

ADR 模板：

```text
# ADR-NNN：标题
状态：proposed / accepted / deprecated / superseded
背景：
决策：
备选方案：
影响：
迁移计划：
验证方法：
回退方法：
```

---

## 14. 面向代码代理的任务标准

给 Codex/Claude 的任务不能只写“帮我把它做完”。推荐使用：

```text
目标：实现什么可观察行为。
允许修改：明确包/文件范围。
禁止修改：状态顺序、frame、碰撞安全、vendor 等边界。
接口：输入、输出、单位、frame 和 owner。
验收：要求运行的测试、MuJoCo 场景和数值门槛。
交付：代码、文档、Git diff、未验证项。
```

代理完成后，负责人至少检查：

1. `git diff --stat` 和关键 diff；
2. 是否新增了隐藏发布者、绝对路径或 fallback；
3. 是否误改 vendor/URDF/安全参数；
4. 测试是否运行当前源码而非旧安装产物；
5. 报告是否区分已验证与未验证；
6. 是否能用一条明确入口在 MuJoCo 复现。

---

## 15. 分阶段整改路线图

### 阶段 A：冻结与可重复（当前首先执行）

1. 冻结当前 `test` 基线并记录可运行场景；
2. 建立 `verification_matrix.md` 和实验 manifest；
3. 修正文档中的参考所有权冲突；
4. 为关键 topic 增加运行时 owner/readiness 检查；
5. 完成 table/blackboard/RAS 的 L2 基线，并重跑桌面任务 L3/L4。

**Go/No-Go：** 没有可复现基线，不开始大规模目录迁移。

### 阶段 B：接口与安全收敛

1. 实现 `tracer_jaka_interfaces` 的结构化状态/错误协议；
2. 实现共享环境碰撞 adapter，移除生产路径 AcceptAll；
3. 固化 ContactSupervisor 行为测试；
4. 定义 tracking/force/replan 滞回协议；
5. 将机器人描述收敛到唯一来源。

**Go/No-Go：** 碰撞、所有权、超时和状态错误码没有自动测试，不进入实机运动。

### 阶段 C：解耦 OCS2/REMANI 与顶层启动

1. 提取 `remani_adapter`；
2. 提取 `ocs2_adapter`；
3. 将 Coordinator 独立为 execution 层；
4. 完整 sim/real launch 移入 bringup；
5. 清理算法包对仿真、地图和硬件包的反向依赖；
6. 建立 vendor patch ledger，决定 REMANI 是否转正式 fork。

**Go/No-Go：** 每迁移一个模块，都必须保持原有 L2/L3 行为，不做一次性“大爆炸”重构。

### 阶段 D：WipePlanner 退役与扩展

1. 将剩余接触监督迁入通用控制/执行层；
2. 对比新旧主链的规划、跟踪和力控指标；
3. 标记 WipePlanner deprecated；
4. 稳定一个发布周期后移入 legacy 或删除；
5. 新任务只通过 YAML/provider/adapter 扩展。

---

## 16. 当前仓库的短期任务清单

按依赖与风险排序：

- [ ] 新建 `docs/verification_matrix.md`，记录各场景 L0–L6 状态；
- [ ] 修正 TA-WBMP `DESIGN.md` 的 MPC 参考所有权描述；
- [ ] 建立 `docs/vendor_patches.md`；
- [ ] 建立 `docs/adr/` 并写第一份“OCS2/REMANI 保持 vendor、通过 adapter 统一”的 ADR；
- [ ] 为 `/mobile_manipulator_mpc_target`、TF 和命令输出实现 readiness 检查；
- [ ] 重跑统一 YAML → TA-WBMP → REMANI → OCS2 的 MuJoCo 无力闭环；
- [ ] 重跑开启力控的桌面接触闭环，保存力、跟踪和接触指标；
- [ ] 实现共享碰撞 adapter，生产配置禁止 AcceptAll；
- [ ] 将状态 String 迁移到结构化接口；
- [ ] 收敛重复 URDF/xacro；
- [ ] 将完整系统 launch 逐步迁至 `tracer_jaka_bringup`；
- [ ] 拆分超大节点文件，并为纯核心补单测；
- [ ] 添加最小 CI、格式检查和贡献指南；
- [ ] 满足退役条件后再处理 WipePlanner。

---

## 17. 最终判断标准

仓库维护良好，不是指“所有代码放进一个框架”或“所有功能都能启动”，而是满足：

1. 一个任务 YAML 可以稳定产生可追踪的任务、规划和执行结果；
2. OCS2、REMANI、TA-WBMP、力控和仿真通过稳定接口协作，内部实现可独立替换；
3. 机器人模型、frame、状态顺序、碰撞语义和参考 owner 只有一个事实源；
4. 每项功能都有明确验证等级、复现实验和失败原因；
5. 仿真先于实机，安全门默认关闭，任何实机放行均有证据；
6. vendor 修改可追溯、可升级、可回退；
7. 项目负责人即使不亲自编写每行代码，也能通过架构、接口和验收标准掌控项目。

