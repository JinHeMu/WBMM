# WBMM 仓库开发与维护标准

> 适用仓库：`/home/a/WBMM`
> 文档级别：工程治理标准
> 当前状态：基于《WBMM架构评估与重构执行方案.md》的小步重构中
> 适用对象：项目负责人、协作者、Codex/Claude 等代码代理

## 1. 文档目的

本标准用于保证 WBMM 在重构过程中：

1. 每项改动可追踪、可验证、可回退；
2. 旧 ROS 主链在未完成迁移前保持可运行；
3. 核心类型、数学、模型、环境、规划、控制职责不继续膨胀；
4. 新增功能不会破坏状态维度、frame、参考所有权和碰撞安全；
5. 所有修改都记录到根目录 `CHANGELOG.md`。

文档优先级：

1. `architecture.md`：系统目标架构、当前主链、模块职责和核心数据合同；
2. `frames.md`：frame、状态向量、速度/力表达和命名规则；
3. `WBMM架构评估与重构执行方案.md`：分模块迁移与验收计划；
4. 本标准：仓库分层、开发流程、验证和发布规则；
5. `CHANGELOG.md`：实际修改历史与后续维护日志；
6. 各功能包 README：包内实现与参数。

## 2. 当前重构原则

当前阶段采用“小步迁移、保留基线”：

- 不一次性删除旧主链；
- 先收敛 `wbmm_core` 和数学，再接入机器人模型、环境、规划和执行；
- Ports 没有真实调用者前冻结，不继续增加业务接口；
- 搜索/优化等业务接口应放在 planning/backend，不放进 core；
- 离线构建关闭 ROS 时不要求 `ament_cmake`；
- 删除旧包前，所有真实使用方必须切换到新位置。

### 已完成

- M01 基础类型与数学：
  - `wbmm_math` 实现迁入 `wbmm_core/math/`
  - 旧 `wbmm_math` 兼容转发包已删除
  - `wbmm_core` 增加 `WBMM_BUILD_OFFLINE=ON` 离线构建选项
  - 差速状态/输入均支持 Eigen round-trip
- 工作区目录收敛：
  - 移除空的 `src/wbmm` 和 `src/interfaces` 骨架
  - 能力目录收敛为 `core/planning/control/visual/map/sim/bringup`

### 正在推进

- M02：机器人模型与碰撞几何
- M03：环境、ESDF 与碰撞检查
- M04+：任务轨迹、规划器、执行协调和接触监督拆分

## 3. 模块职责

| 模块 / 目录 | 职责 | 当前处理 |
|---|---|---|
| `src/core` | `wbmm_core`：统一状态/结果/校验/math，少量稳定接口 | 唯一基础库，继续收敛 |
| `src/planning/ta_wbmp` | 任务轨迹、候选规划、执行协调 | 后续按真实主链收敛 |
| `src/control/tracer_jaka_ocs2` | OCS2 MPC/MRT 集成 | 保留独立集成包 |
| `src/control/whole_body_force_control` | 导纳/恒力/力跟随 | 复用数值库，迁移接触监督 |
| `src/visual` | `wbmm_visualization` 统一机器人/轨迹显示 | 保留，消费统一输出 |
| `src/applications/wiping/wipe_planner` | 旧接触执行基线 | 冻结/回归基线 |
| `src/robot` | 机器人描述、模型、URDF | 收敛为唯一模型来源 |
| `src/drivers` | 底盘、机械臂、传感器驱动 | 保留边界 |
| `src/map` | ESDF、地图、定位 | 统一查询语义 |
| `src/sim` | 仿真后端容器 | 保留 MuJoCo 与未来仿真实现 |
| `src/bringup` | `tracer_jaka_bringup` 顶层 launch 与部署 | 唯一完整系统组合入口 |

## 4. 目标目录

```text
src/
├── core/                       # 唯一 wbmm_core 包
├── planning/                   # 规划包容器
├── control/                    # 控制与重依赖集成包容器
├── visual/                     # 唯一 wbmm_visualization 包
├── map/                        # 定位、建图、ESDF 包容器
├── sim/                        # 仿真后端包容器
├── applications/               # 具体任务
├── robot/                      # 机器人描述与 MoveIt 配置
├── drivers/                    # 硬件驱动
├── bringup/                    # 唯一 tracer_jaka_bringup 包
└── vendor/                     # 上游工程与重依赖
```

目录规则：

- 系统内唯一能力（`core`、`visual`、`bringup`）可直接作为 ROS 包根；
- 可容纳多个实现的能力（`planning`、`control`、`map`、`sim`）作为包容器；
- `robot`、`drivers`、`applications`、`vendor` 保持边界，不为缩短路径合并包；
- 不建立空的主程序包；WbmmNode/FSM/Manager 有真实实现时再创建主链。

## 5. 不可改变的基础合同

### 5.1 状态 / 控制维度

```text
x = [base_x, base_y, base_yaw, q1, q2, q3, q4, q5, q6]  # 9D
u = [base_v, base_yaw_rate, qdot1, qdot2, qdot3, qdot4, qdot5, qdot6]  # 8D
```

### 5.2 差速底盘

- 禁止横向速度；
- 所有底盘路径/参考必须满足差速非完整约束。

### 5.3 Frame / 单位 / 时间

- 所有空间量必须声明 frame；
- 所有时间量必须声明 clock；
- 四元数内部使用 `wxyz`，ROS 边界转换 `xyzw`；
- 关节数据必须携带名称，禁止猜测顺序。

### 5.4 参考所有权

同一时刻只能有一个 MPC/执行参考所有者：

- `NAVIGATING`：导航/REMANI 后端；
- `TASK_EXEC`：执行协调器或 WbmmManager；
- 切换必须有请求、确认、超时和发布者数量检查。

## 6. 依赖方向

```text
applications / bringup
        ↓
execution / planning / control / map
        ↓
robot model / wbmm_core
        ↓
vendor / 具体求解器 / 驱动
```

禁止：

- `wbmm_core` 依赖 ROS、具体机器人、vendor；
- vendor 反向依赖 application；
- 算法包直接依赖实机设备参数；
- 规划器自行启动整套机器人；
- 为让规划成功而关闭碰撞或缩小真实碰撞体。

## 7. 开发流程

### 7.1 变更前

1. 阅读 `architecture.md`、`frames.md`、相关包 README；
2. 查看 `CHANGELOG.md` 最近改动；
3. 记录当前 Git SHA 与最小验证基线；
4. 区分：Bug 修复 / 参数调整 / 接口变化 / 重构 / 新功能；
5. 接口或架构变化先写 ADR。

### 7.2 实现中

- 一次提交解决一个可解释问题；
- 先写纯核心和测试，再接 ROS adapter；
- 不修改无关文件；
- 不悄悄删除兼容层；
- 不把“能编译”当作“已迁移完成”。

### 7.3 提交前

```bash
git diff --check
colcon build --packages-select <changed_packages> --symlink-install
colcon test --packages-select <changed_packages>
colcon test-result --verbose
```

核心/数学改动还需运行离线构建：

```bash
cmake -S src/core -B build/wbmm_core_offline \
  -DWBMM_BUILD_OFFLINE=ON -DBUILD_TESTING=ON
cmake --build build/wbmm_core_offline
ctest --test-dir build/wbmm_core_offline --output-on-failure
```

### 7.4 修改记录

每次修改必须在根目录 `CHANGELOG.md` 追加记录，至少包含：

- 日期与范围；
- 改动原因；
- 关键文件；
- 验证结果；
- 未验证/后续工作。

## 8. 验证分级

| 等级 | 名称 | 证据 |
|---|---|---|
| L0 | 合同检查 | schema、frame、单位、owner、维度、参数合法性 |
| L1 | 单元测试 | 纯 C++/核心测试，错误路径 |
| L2 | 离线规划 | YAML 输入、候选/失败原因、轨迹、碰撞检查 |
| L3 | MuJoCo 无力闭环 | 导航、交权、OCS2 跟踪、无碰撞 |
| L4 | MuJoCo 接触闭环 | 力跟踪、接触保持、尖峰/超时/硬限位 |
| L5 | 实机只读 | TF、状态、传感器、无命令输出 |
| L6 | 实机运动 | 风险评审、低速分阶段放行、日志复盘 |

报告中必须区分：

- 已实现
- 已构建
- 已离线验证（L0–L2）
- 已在 MuJoCo 闭环验证（L3/L4）
- 已实机只读验证（L5）
- 已实机运动验证（L6）
- 未验证

## 9. 分支与发布

建议分支：

- `main`：稳定基线；
- `test`：集成验证；
- `feature/<topic>` 或 `fix/<topic>`：单功能/修复；
- `experiment/<topic>`：研究试验，不作为稳定依赖。

发布 tag：

```text
sim-vX.Y.Z       # 至少 L3，接触功能至少 L4
real-ro-vX.Y.Z   # 至少 L5
real-vX.Y.Z      # 至少 L6
```

## 10. 删除旧模块/文件的条件

- 新实现已承载旧模块的职责；
- 所有真实调用者已切换；
- 回归基线已保存；
- 修改记录已写入 `CHANGELOG.md`；
- 不再依赖旧文件中的唯一事实源。

当前已经过期的旧目录设计文件 `工程目录重构设计.md` 已从根目录移除，其有效内容已合并到本文件、`architecture.md` 与 `CHANGELOG.md`。

## 11. 最终判断标准

仓库维护良好的标志：

1. 每个模块有明确职责和 owner；
2. 每次修改有记录、有测试、有验证等级；
3. 核心不依赖 ROS；
4. 状态、frame、参考所有权和碰撞语义只有唯一事实源；
5. 旧主链可运行，新核心逐步接入；
6. 删除旧模块前有明确切换证据。
