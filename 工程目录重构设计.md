# WBMM 工程目录简化设计

> 本文只规定目录、依赖和文件放置。总体运行架构见 `docs/WBMM重新架构与代码掌控计划.md`。

---

## 1. 目录目标

目录必须做到：

1. 从入口可以顺序找到状态机、Manager、算法和 backend；
2. 通用包名和目录名不包含具体机器人名称；
3. 新任务、新算法和新机器人都有固定放置位置；
4. 科研阶段可以用一个 launch 快速部署；
5. 不提前创建没有代码和使用者的包。

---

## 2. 目标目录

```text
WBMM/
├── README.md
├── docs/
│   ├── WBMM重新架构与代码掌控计划.md
│   ├── architecture_decisions.md       # 简短决策记录
│   └── verification.md                 # L0–L6 验证状态
├── data/                               # rosbag、地图、日志和生成结果
├── tools/                              # 独立诊断与转换脚本
│
└── src/
    ├── core/
    │   └── wbmm_core/                  # 基础 C++ 库
    │       ├── CMakeLists.txt
    │       ├── package.xml
    │       ├── include/wbmm_core/
    │       │   ├── types.hpp
    │       │   ├── result.hpp
    │       │   ├── validation.hpp
    │       │   ├── interfaces.hpp
    │       │   └── math/
    │       │       ├── conversions.hpp
    │       │       └── linear_algebra.hpp
    │       ├── src/
    │       └── test/
    │
    ├── wbmm/                           # 唯一主程序包
    │   ├── CMakeLists.txt
    │   ├── package.xml
    │   ├── include/wbmm/
    │   │   ├── manager.hpp
    │   │   ├── fsm.hpp
    │   │   ├── planning/
    │   │   ├── control/
    │   │   └── backends/
    │   ├── src/
    │   │   ├── node.cpp               # 唯一 ROS 主入口
    │   │   ├── manager.cpp            # 唯一业务编排入口
    │   │   ├── fsm.cpp                # 阶段、重规划和故障
    │   │   ├── planning/
    │   │   ├── control/
    │   │   └── backends/
    │   │       ├── planner_backend.cpp
    │   │       ├── controller_backend.cpp
    │   │       ├── robot_model.cpp
    │   │       └── environment.cpp
    │   ├── config/
    │   │   ├── common.yaml
    │   │   ├── profiles/
    │   │   │   └── platform_v1/
    │   │   │       ├── robot.yaml
    │   │   │       ├── joints.yaml
    │   │   │       ├── frames.yaml
    │   │   │       └── controllers.yaml
    │   │   └── tasks/
    │   │       └── wiping.yaml
    │   ├── launch/
    │   │   ├── sim.launch.py
    │   │   └── real.launch.py
    │   ├── rviz/
    │   └── test/
    │
    └── vendor/                         # 外部工程，保留原始包名
        ├── remani_planner/
        └── ocs2_ros2/
```

这是目标形态，不要求一次性移动现有仓库。只有代码迁入时才建立相应目录。

---

## 3. 为什么只保留两个 WBMM 自有包

### `wbmm_core`

它需要被算法、测试和未来其他程序复用，因此独立成包。允许依赖：

```text
C++17
Eigen3
```

不允许依赖：

```text
rclcpp / ROS messages / TF
具体机器人 description
硬件 SDK
REMANI / OCS2 / MuJoCo
```

领域数据仍使用普通 C++ 类型；Eigen 只出现在 `math/` 和确实需要矩阵表达的接口中。

### `wbmm`

规划、控制、运行状态机、ROS 转换、backend 和 bringup 在科研阶段共同服务于同一个可执行程序，因此先放在一个包中。它们通过 C++ 目录分开，不通过 ROS 包分开。

---

## 4. 依赖方向

```text
WbmmNode → WbmmFSM → WbmmManager
                         ├── planning
                         ├── control
                         └── backends → vendor / ROS / robot / environment

wbmm package ──depends──> wbmm_core
backends     ──implement→ wbmm_core interfaces
```

硬规则：

- `wbmm_core` 不能依赖 `wbmm`；
- `vendor` 不能依赖 WBMM application；
- planning/control 不直接读取 ROS topic；
- ROS 消息转换只出现在 `node.cpp` 或 `src/backends/`；
- 具体机器人差异只能进入 `config/profiles/` 和 RobotModel 实现；
- Manager 不出现设备 IP、CAN 名称和具体关节常量。

---

## 5. 文件放置规则

| 内容 | 放置位置 |
|---|---|
| 通用状态、轨迹、错误码 | `wbmm_core/include/wbmm_core/` |
| Eigen 转换、伪逆、SE(3) 工具 | `wbmm_core/include/wbmm_core/math/` |
| 主流程编排 | `wbmm/src/manager.cpp` |
| 阶段和故障切换 | `wbmm/src/fsm.cpp` |
| ROS subscription/publisher/TF | `wbmm/src/node.cpp` |
| 规划算法 | `wbmm/src/planning/` |
| 控制算法 | `wbmm/src/control/` |
| 外部库调用 | `wbmm/src/backends/` |
| 机器人关节、frame、限制 | `wbmm/config/profiles/<platform_id>/` |
| 任务轨迹和任务参数 | `wbmm/config/tasks/` |
| 系统组合 | `wbmm/launch/` |
| 上游源码 | `src/vendor/` |

判断原则：

> 如果代码回答“算法怎么计算”，放 planning/control；如果回答“如何调用外部系统”，放 backends；如果回答“何时调用谁”，放 Manager/FSM。

---

## 6. 命名规则

允许：

```text
wbmm_core
wbmm
planner_backend.cpp
robot_model.cpp
platform_v1
```

通用路径中禁止：

- 机器人厂商或型号；
- 设备 IP、CAN 口或序列号；
- 某一平台的关节名称；
- `sim`/`real` 条件分支散落在算法代码中。

第三方算法名称可以出现在 `vendor/` 内，或者作为 backend 类名出现，因为它表达的是可替换算法实现，不是机器人身份。

---

## 7. 配置规则

配置读取顺序固定：

```text
common.yaml
  → profiles/<platform_id>/*.yaml
  → tasks/<task>.yaml
  → launch arguments
```

`common.yaml` 保存平台无关参数：backend 选择、规划 horizon、通用安全阈值、FSM 超时和可视化开关。

`profiles/<platform_id>/` 保存平台差异：URDF/SRDF、joint order、frames、limits、controllers、传感器和标定。

`tasks/` 保存任务差异：目标几何、接触方向、期望力、进入/执行/退出阶段和任务容差。

配置合并后必须输出最终快照，保证实验可复现。

---

## 8. 代码阅读顺序

```text
src/node.cpp
  → src/fsm.cpp
  → src/manager.cpp
  → src/planning/ 或 src/control/
  → src/backends/
  → vendor（只有研究外部算法时才进入）
```

禁止出现必须跨十几个包才能理解一次规划请求的数据流。

---

## 9. 构建与部署

```bash
colcon build --packages-up-to wbmm
source install/setup.bash

ros2 launch wbmm sim.launch.py \
  profile:=platform_v1 \
  task:=wiping
```

实机入口：

```bash
ros2 launch wbmm real.launch.py \
  profile:=platform_v1 \
  task:=wiping \
  command_output:=false
```

默认实机不输出运动命令。

---

## 10. 何时允许再拆包

| 触发现象 | 处理方式 |
|---|---|
| 消息被多个独立 node 使用 | 拆出 `wbmm_interfaces` |
| ROS converters 在多个程序重复 | 拆出 `wbmm_ros` |
| backend 需要可选安装或独立发布 | 拆出对应 adapter |
| 硬件插件需要 pluginlib/ros2_control 加载 | 拆出 hardware 包 |
| 仿真资产需要独立分发 | 拆出 simulation 包 |
| 单包依赖或编译时间严重影响开发 | 拆 planning/control |

没有可观察到的问题时不拆。

---

## 11. 当前目录与目标的差异

当前已经存在：

```text
src/core/wbmm_core/
src/core/wbmm_math/
大量现有 algorithms、applications、bringup、robot 和 simulation 包
```

目标设计决定：

- `wbmm_math` 最终合入 `wbmm_core/math/`；
- 当前 `wbmm_core` 的较多 Ports 最终裁剪为四个稳定接口；
- 现有包不立即删除，先通过 `wbmm` 主程序逐步接入；
- 旧链在新链达到对应证据等级前继续作为基线。

本轮只更新设计，没有执行上述移动、合并或删除。

---

## 12. 最终检查标准

一个新成员应该能在半小时内回答：

1. 系统入口在哪里？——`wbmm/src/node.cpp`；
2. 谁控制流程？——`WbmmFSM`；
3. 谁串联算法？——`WbmmManager`；
4. 规划和控制代码在哪里？——`planning/` 与 `control/`；
5. 外部库在哪里调用？——`backends/`；
6. 如何换机器人？——修改 profile 和 RobotModel；
7. 如何启动？——一个 `sim.launch.py` 或 `real.launch.py`。

如果这些问题不能快速回答，说明目录再次变复杂，需要先合并而不是继续拆包。
