# 前期工作汇报：移动机械臂全身运动优化

## 1. 代码 pipeline

已基本梳理 WBMM（Whole-Body Mobile Manipulation）的代码框架、pipeline 与核心接口。

核心模块：`core` 数据结构与接口 · `robotics` 运动学 · `planning` 前端/后端规划 · `control` MPC/力控 · `map` 定位/ESDF · `drivers` 实机驱动 · `sim` MuJoCo · `bringup` 部署。

```mermaid
flowchart LR
    G[全身目标] --> B[前端规划与后端优化]
    B --> M[OCS2 MPC]
    M --> T[MRT 执行]
    T --> H[实机或 MuJoCo]
    H --> T
```

## 2. 后端优化两条主线

1. **全身协调控制**：机器人受外力扰动时，合理分配底盘与机械臂运动，保证末端任务完成。
2. **长时域联合规划**：将全身导航与执行统一优化，生成高效、可执行的全身轨迹。

## 3. 评价指标

统一采用经过任务选择和尺度归一化的可执行雅可比 $J$：移动全身运动用 $6\times8$ 雅可比，底盘固定时用机械臂雅可比。奇异值满足 $\sigma_1\ge\cdots\ge\sigma_m$。

### Part I：瞬时全身协调与分配

| 指标 | 作用 |
|---|---|
| 最小奇异值 $\sigma_{\min}$ | 最弱方向运动能力，核心指标 |
| 任务方向可操作度 $\rho_d$ | 指定方向运动能力，核心指标 |
| 底盘/机械臂利用率 $\rho_b,\rho_a$ | 分别判断两侧是否接近限幅 |
| 分配裕量 $M_{\rm alloc}=1-\max(\rho_b,\rho_a)$ | 全身输入剩余空间 |
| 关节限位裕量 $m_j$ | 机械臂距关节边界的调整空间 |
| 碰撞安全裕量 $m_c$ | 全身避障裕量 |

关键公式：

$$
\rho_i=\frac{|u_i|}{u_i^{\max}},\qquad
m_j=\min_i\frac{\min(q_i-q_i^{\min},\,q_i^{\max}-q_i)}{q_i^{\max}-q_i^{\min}}.
$$

### Part II：长时域导航 + 执行联合优化

| 指标 | 作用 |
|---|---|
| 成功率、任务进度、终端误差 | 任务是否完成 |
| 时间 $T$、路径 $L$、能耗 $E$ | 导航与执行效率 |
| 全过程最小奇异裕量 $M_\sigma=\min_t\sigma_{\min}(t)$ | 最弱时刻的运动能力 |
| 全过程最小关节裕量 $M_j=\min_t m_j(t)$ | 最接近关节边界的时刻 |
| 全过程最小安全裕量 $M_c=\min_t m_c(t)$ | 最危险时刻的避障裕量 |
| 近奇异时间比例 $P_{\rm near}=\frac1T\int_0^T\mathbf 1(\sigma_{\min}<\sigma_{\rm warn})dt$ | 危险区域持续时间 |
| 近饱和/近碰撞时间比例 $P_{\rm sat},P_{\rm coll}$ | 输入和碰撞风险占比 |
| 分配平滑度 $S_{\rm alloc}$、前瞻底盘重定位收益 $G_{\rm pre}$ | 减少频繁切换，鼓励底盘提前调整 |

> 优先使用：**最小奇异值 + 关节限位裕量 + 任务方向可操作度 + 全过程瓶颈指标**。条件数和倒条件数仅作诊断；阈值需在尺度固定后确定。

## 4. 下一步

1. 完成全身协调控制与 MPC 接口闭环；
2. 在仿真中验证可操作度、限位、避障指标；
3. 推进导航 + 执行的长时域联合优化。
