# Real OCS2 overrides. Common structure and topic/frame names live in
# config/common/ocs2.yaml.
wbmm_mpc_node:
  ros__parameters:
    use_sim_time: false

wbmm_mrt_node:
  ros__parameters:
    use_sim_time: false
    command_output_enabled: false

wbmm_target_node:
  ros__parameters:
    use_sim_time: false
