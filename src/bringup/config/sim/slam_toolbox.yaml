# MuJoCo slam_toolbox overrides. Common mapping parameters live in
# config/common/slam_toolbox.yaml.
slam_toolbox:
  ros__parameters:
    use_sim_time: true
    max_laser_range: 8.0
    minimum_time_interval: 0.10
    scan_buffer_maximum_scan_distance: 10.0
